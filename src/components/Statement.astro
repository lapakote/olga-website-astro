<section class="statement" aria-label="Цитата">
  <div class="statement__inner">
    <span class="statement__mark" aria-hidden="true">«</span>
    <p class="statement__quote reveal" style="--i:0">Наука: лучшая база для бизнеса в XXI веке.</p>
    <p class="statement__attribution reveal" style="--i:1">Ольга Абрамова</p>
  </div>
</section>

<style>
  .statement {
    background: var(--color-ink);
    padding: clamp(5rem, 11vw, 8rem) clamp(1.25rem, 6vw, 5rem);
  }

  .statement__inner {
    max-width: 56rem;
    margin: 0 auto;
  }

  .statement__mark {
    display: block;
    font-family: var(--font-display);
    font-weight: 900;
    font-size: clamp(3rem, 8vw, 5rem);
    line-height: 1;
    color: var(--color-accent);
    margin-bottom: clamp(0.5rem, 2vw, 1rem);
  }

  .statement__quote {
    margin: 0;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: clamp(2rem, 5.5vw, 3.75rem);
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--color-bg);
    text-wrap: balance;
  }

  .statement__attribution {
    margin: clamp(1.5rem, 4vw, 2rem) 0 0;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 0.9375rem;
    color: color-mix(in oklab, var(--color-bg) 65%, transparent);
  }
</style>
